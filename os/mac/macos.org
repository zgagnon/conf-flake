* MacOS
:PROPERTIES:
:header-args: :tangle default.nix
:END:

#+begin_src nix
{ pkgs,... }:
{
  security.pam.enableSudoTouchIdAuth = true;
  system.defaults = {
    dock.autohide = true;
  };

  fonts.packages = with pkgs; [
    (nerdfonts.override {
      fonts = [
        "FiraCode"
        "DroidSansMono"
      ];
    })
  ];
}
#+end_src
